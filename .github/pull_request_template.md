<!--
Delete any heading below that this change has nothing to put under.
Keep `Closes` last, so it still parses when this description becomes the
squash commit body.
-->

## What changed

<!-- What is wrong now, and what this does about it. -->

## Why this way

<!-- The constraint that forced the choice, or the alternative rejected and
why. Skip it where the diff already answers the question. -->

## Verification

<!-- What you checked beyond `mix test`, `mix compile --warnings-as-errors`,
`mix format --check-formatted`, and `mix credo`, which CI runs anyway. -->

## Known gaps

<!-- What this leaves unfinished on purpose, and what would close it. -->

Closes #
